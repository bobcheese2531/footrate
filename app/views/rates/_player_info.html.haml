= image_tag team_logo(p.team_id), size: "30x30"
%strong= p.shirtnumber
%strong= p.position 
%strong= p.name
%p.alert-link.mt-2= "点数：#{p.player_rate}"
%p.alert-link コメント：
= p.rate_text.present? ? (simple_format(p.rate_text)) : ("なし")