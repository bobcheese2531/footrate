%h1.midasi 順位表
%div.container-fluid
  %div.row.d-flex.align-items-center.justify-content-center
    - standings.each do |s|
      %div.col-xs-12.mx-3.my-3
        %h5.text-center.midasi= s['group']
        %table.table
          %thead
            %tr.text-center
              %th 順位
              %th クラブ
              %th 試合
              %th 勝
              %th 分
              %th 負
              %th Pts
          %tbody
            - s['table'].each do |t|
              %tr.text-center
                %td= t['position']
                %td= image_tag team_logo(t['team']['id']), size: "30x30"
                %td= t['playedGames']
                %td= t['won']
                %td= t['draw']
                %td= t['lost']
                %td= t['points']