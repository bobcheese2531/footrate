<tbody>
  <tr>
    
    <td>
      <%= profile_image(user) %>
    </td>
    
    <% if other_user?(user) %>
      <td><%= link_to 'チャットする', chat_path(user.id), class: "btn btn-outline-success" %></td>
    <% end %>
    
  </tr>
</tbody>