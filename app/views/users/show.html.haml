%h2
  = @user.name
  のページ

= link_to "編集", edit_user_path(@user)

%div.container.mt-3
  %h2.text-center 
    = image_tag "https://crests.football-data.org/#{@team["id"]}.svg", size: "50x50"
    = @team["name"]
    = "の試合"