<!--見出し-->
<div class="container">
  <div class="row justify-content-between">
    <h1>評価一覧</h1>
    <%= render 'search' %>
  </div>
</div>

<!--投稿一覧表示-->
<%= render 'show_all' %>

<!--ページネーション-->
<div class="container">
  <%= paginate @games %>
</div>