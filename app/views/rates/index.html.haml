// title
%h1.text-center= link_to "投稿一覧", rates_path, class: "midasi"

// search form
= render 'search'

// rates      
%div.container.mt-3  
  = render 'rate_card', rates: @rates 

// pagination  
%div.mt-3  
  = paginate @rates
  