%h2.border-bottom
  = image_tag "https://crests.football-data.org/#{team["id"]}.svg", size: "30x30"
  = team["name"]
%div.container
  %div.row
    - side.each do |s|
      = form.fields_for :players do |f|
        = render 'field_form', {f: f, player: s, team_id: team["id"]}